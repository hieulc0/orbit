[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "orbit-worker"
version = "0.1.0"
description = "Orbit worker protocol client"
requires-python = ">=3.10"
license = {text = "Apache-2.0"}

[tool.setuptools]
py-modules = ["orbit_worker"]
